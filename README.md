A test of integrating using Open AI for translating an application.

Requires an Open AI API key defined in the `OPENAI_KEY` environment variable

Then run `mvn`, open the dev tools window and the `code` tab.
